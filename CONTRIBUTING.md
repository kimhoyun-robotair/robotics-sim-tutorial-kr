# 기여 가이드

이 저장소는 Ubuntu 24.04 LTS, ROS 2 Jazzy, Gazebo Harmonic 조합의 한국어 튜토리얼입니다. 문서와 예제는 함께 변경하고, 문서의 명령은 가능한 한 실제 환경에서 확인합니다.

기여하는 코드는 저장소의 [Apache License 2.0](LICENSE) 조건으로 배포할 수 있어야 합니다.

## 문서 작성

- 설명은 한국어로 작성하고, 패키지명·토픽명·CLI 명령은 원문 표기를 유지합니다.
- 새 문서는 `docs/` 아래 영문 소문자 kebab-case 파일명으로 만듭니다.
- 각 실습은 학습 목표, 실행, 결과 확인, 동작 원리, 문제 해결, 다음 단계를 포함합니다.
- 이미지와 다이어그램은 반드시 `docs/images/` 아래에 둡니다.
- 긴 코드 전체를 Markdown에 복제하지 말고, 저장소의 실제 파일 경로를 안내합니다.

## 예제 작성

- 공통 로봇은 `tutorial_bot`이며, 별도 로봇을 무분별하게 추가하지 않습니다.
- 순수 Gazebo 예제는 `examples/gazebo/`, ROS 2 예제는 `examples/ros2_ws/`에 둡니다.
- URDF/Xacro를 로봇 설명의 원본으로 사용하고, 같은 로봇의 별도 SDF 원본을 중복 관리하지 않습니다.
- `build/`, `install/`, `log/`, `site/`는 커밋하지 않습니다.

## 외부 자료

외부 저장소는 구성과 주제 순서를 이해하는 참고 자료로만 사용합니다. 복제본이 필요하면 `ref/`에만 두며, 이 디렉터리는 Git에서 제외하고 절대 편집하지 않습니다. 외부 코드·이미지·문장을 복사하지 말고, 이 저장소의 환경과 교육 흐름에 맞는 독자적인 설명과 예제를 작성합니다.

## 확인

문서 변경 후 다음을 실행합니다.

```bash
python3 -m mkdocs build --strict
```
